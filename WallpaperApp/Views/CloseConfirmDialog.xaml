<Window x:Class="WallpaperApp.Views.CloseConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WallpaperApp.ViewModels"
        Title="GNIB Wallpaper"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#1E1E1E"
        Foreground="#EEEEEE">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="140"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#EEEEEE"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Closing GNIB Wallpaper"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1"
                   Text="Do you want to minimize the app to the system tray, or exit completely?"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>

        <StackPanel Grid.Row="2">
            <CheckBox x:Name="RememberCheckBox"
                      Content="Remember my choice"
                      IsChecked="{Binding Remember, Mode=TwoWay}"
                      Foreground="#CCCCCC"
                      Margin="0,0,0,16"/>
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="MinimizeButton"
                        Content="Minimize to tray"
                        IsDefault="True"
                        Click="OnMinimizeClick"/>
                <Button x:Name="ExitButton"
                        Content="Exit application"
                        Click="OnExitClick"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
